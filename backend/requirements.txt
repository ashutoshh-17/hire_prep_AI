# ── Runtime (backend server) ─────────────────────────────────────────
fastapi
uvicorn
python-multipart
pypdf
python-dotenv
spacy[transformers]
sentence-transformers

# PyTorch — CPU inference
torch
torchvision

# ── Training only (reinstall when retraining the NER model) ──────────
# pandas
# datasets
# scikit-learn
